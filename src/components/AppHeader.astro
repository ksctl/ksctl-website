---
import Container from "./Container.astro";

const currentPath = Astro.url.pathname;

function isActivePath(linkPath: string) {
  if (linkPath === '/') return currentPath === '/';
  return currentPath.startsWith(linkPath);
}

const navigation = {
  links: [
    {
      type: "dropdown",
      label: "Products",
      items: [
        { to: "/product/kubmin", label: "kubmin", description: "Kubernetes Efficiency Platform" },
        { to: "/product/kli", label: "kli", description: "Open-source Cluster CLI" }
      ]
    },
    { type: "link", to: "/pricing", label: "Pricing" },
    { type: "link", to: "/blog", label: "Blog" },
    { type: "link", to: "/aboutus", label: "About" },
    { type: "link", to: "https://docs.ksctl.com", label: "Docs", external: true },
    { type: "link", to: "https://github.com/ksctl", label: "GitHub", external: true }
  ]
};
---

<script>
  let isToggled = false;
  const navlinks = document.querySelector("#navlinks");
  const hamburger = document.querySelector("#hamburger");
  const layer = document.querySelector("#navLayer");
  // @ts-ignore
  const links = [...navlinks.querySelector("ul").children];

  function toggleNavlinks() {
    if (isToggled) {
      navlinks.classList.add(
        "!visible",
        "!scale-100",
        "!opacity-100",
        "!lg:translate-y-0"
      );
      hamburger.classList.add("toggled");
      layer.classList.add("origin-top", "scale-y-100");
    } else {
      navlinks.classList.remove(
        "!visible",
        "!scale-100",
        "!opacity-100",
        "!lg:translate-y-0"
      );
      hamburger.classList.remove("toggled");
      layer.classList.remove("origin-top", "scale-y-100");
    }
  }
  hamburger.addEventListener("click", () => {
    isToggled = !isToggled;
    toggleNavlinks();
  });

  links.forEach((link) => {
    link.addEventListener("click", () => {
      isToggled = false;
      toggleNavlinks();
    });
  });
</script>

<header>
  <nav
    class="fixed z-40 w-full min-h-[72px] border-b border-gray-800/50 backdrop-blur-lg bg-ksctl-bg/80 transition-all duration-300 font-mono"
  >
    <Container>
      <div
        class="relative flex flex-wrap items-center justify-between gap-6 py-5 md:gap-0 md:py-6"
      >
        <div class="relative z-20 flex w-full justify-between md:px-0 lg:w-max">
          <a href="/" aria-label="logo" class="flex items-center space-x-2.5 group">
              <img
                src="/ksctl-logo.svg"
                alt="ksctl logo"
                width="32"
                height="32"
                class="brightness-110"
              />
            <span class="text-xl font-bold text-ksctl-text-primary tracking-tight">ksctl</span>
          </a>

          <div class="relative flex max-h-10 items-center lg:hidden">
            <button
              aria-label="humburger"
              id="hamburger"
              class="relative -mr-6 p-6"
            >
              <div
                aria-hidden="true"
                id="line"
                class="m-auto h-0.5 w-5 rounded bg-ksctl-text-primary transition duration-300"
              >
              </div>
              <div
                aria-hidden="true"
                id="line2"
                class="m-auto mt-2 h-0.5 w-5 rounded bg-ksctl-text-primary transition duration-300"
              >
              </div>
            </button>
          </div>
        </div>
        <div
          id="navLayer"
          aria-hidden="true"
          class="fixed inset-0 z-10 h-screen w-screen origin-bottom scale-y-0 bg-ksctl-bg/90 backdrop-blur-2xl transition duration-500 lg:hidden"
        >
        </div>
        <div
          id="navlinks"
          x-data="{ dropdownOpen: false, mobileDropdownOpen: false }"
          @click.away="dropdownOpen = false"
          class="w-max right-1 invisible absolute top-full z-20 origin-top-right translate-y-1 scale-90 flex-col flex-wrap justify-center gap-6 rounded-xl border border-gray-800 bg-[#0d1117] p-6 opacity-0 shadow-2xl transition-all duration-300 lg:visible lg:relative lg:flex lg:w-auto lg:translate-y-0 lg:scale-100 lg:flex-row lg:items-center lg:gap-0 lg:border-none lg:bg-transparent lg:p-0 lg:opacity-100 lg:shadow-none lg:py-3"
        >
          <div
            class="w-full text-ksctl-text-secondary lg:w-auto lg:pr-4 lg:pt-0"
          >
            <ul
              class="flex flex-col gap-6 tracking-wide lg:flex-row lg:gap-0 lg:text-sm"
            >
              {
                navigation.links.map((link) => {
                  if (link.type === "dropdown") {
                    return (
                      <li class="relative">
                        {/* Desktop Dropdown */}
                        <div class="hidden lg:block">
                          <button
                            @mouseenter="dropdownOpen = true"
                            @mouseleave="dropdownOpen = false"
                            class="hover:text-primary flex items-center transition font-medium md:px-4"
                          >
                            <span>{link.label}</span>
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-1" viewBox="0 0 20 20" fill="currentColor">
                              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
                            </svg>
                          </button>

                          {/* Dropdown Menu */}
                          <div
                            x-show="dropdownOpen"
                            @mouseenter="dropdownOpen = true"
                            @mouseleave="dropdownOpen = false"
                            x-transition:enter="transition ease-out duration-200"
                            x-transition:enter-start="opacity-0 translate-y-1"
                            x-transition:enter-end="opacity-100 translate-y-0"
                            x-transition:leave="transition ease-in duration-150"
                            x-transition:leave-start="opacity-100 translate-y-0"
                            x-transition:leave-end="opacity-0 translate-y-1"
                            class="absolute left-0 mt-2 w-64 rounded-xl bg-[#0d1117] border border-gray-800 shadow-xl overflow-hidden"
                            style="display: none;"
                          >
                            {link.items.map((item) => (
                              <a
                                href={item.to}
                                class:list={[
                                  "block px-4 py-3 hover:bg-gray-800/50 transition-colors duration-200",
                                  isActivePath(item.to) && "bg-primary/5"
                                ]}
                              >
                                <div class="flex items-start justify-between">
                                  <div>
                                    <div class:list={[
                                      "font-semibold text-ksctl-text-primary",
                                      isActivePath(item.to) && "text-primary"
                                    ]}>
                                      {item.label}
                                    </div>
                                    <div class="text-xs text-ksctl-text-secondary mt-0.5">
                                      {item.description}
                                    </div>
                                  </div>
                                </div>
                              </a>
                            ))}
                          </div>
                        </div>

                        {/* Mobile Accordion */}
                        <div class="lg:hidden">
                          <button
                            @click="mobileDropdownOpen = !mobileDropdownOpen"
                            class="w-full hover:text-primary flex items-center justify-between transition font-medium"
                          >
                            <span>{link.label}</span>
                            <svg
                              xmlns="http://www.w3.org/2000/svg"
                              class="h-4 w-4 transition-transform duration-200"
                              :class="{'rotate-180': mobileDropdownOpen}"
                              viewBox="0 0 20 20"
                              fill="currentColor"
                            >
                              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
                            </svg>
                          </button>

                          <div
                            x-show="mobileDropdownOpen"
                            x-transition:enter="transition ease-out duration-200"
                            x-transition:enter-start="opacity-0 -translate-y-1"
                            x-transition:enter-end="opacity-100 translate-y-0"
                            class="mt-3 ml-4 space-y-3 border-l-2 border-primary/20 pl-4"
                            style="display: none;"
                          >
                            {link.items.map((item) => (
                              <a
                                href={item.to}
                                class:list={[
                                  "block text-sm transition-colors duration-200",
                                  isActivePath(item.to)
                                    ? "text-primary font-semibold"
                                    : "text-ksctl-text-secondary hover:text-primary"
                                ]}
                              >
                                <div class="font-medium">{item.label}</div>
                              </a>
                            ))}
                          </div>
                        </div>
                      </li>
                    );
                  } else {
                    return (
                      <li>
                        <a
                          href={link.to}
                          target={link.external ? "_blank" : undefined}
                          rel={link.external ? "noopener noreferrer" : undefined}
                          class:list={[
                            "block transition font-medium md:px-4",
                            isActivePath(link.to)
                              ? "text-primary"
                              : "hover:text-primary"
                          ]}
                        >
                          <span>{link.label}</span>
                        </a>
                      </li>
                    );
                  }
                })
              }
            </ul>
          </div>
        </div>
      </div>
    </Container>
  </nav>
</header>

<style>
  #toggle_nav:checked ~ div #hamburger #line {
    @apply translate-y-1.5 rotate-45;
  }

  #toggle_nav:checked ~ div #hamburger #line2 {
    @apply -translate-y-1 -rotate-45;
  }

  .toggled div:first-child {
    @apply translate-y-1.5 rotate-45;
  }
  .toggled div:last-child {
    @apply -translate-y-1 -rotate-45;
  }
</style>
